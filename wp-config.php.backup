<?php
/**
 * The base configuration for WordPress
 *
 * The wp-config.php creation script uses this file during the
 * installation. You don't have to use the web site, you can
 * copy this file to "wp-config.php" and fill in the values.
 *
 * This file contains the following configurations:
 *
 * * MySQL settings
 * * Secret keys
 * * Database table prefix
 * * ABSPATH
 *
 * @link https://codex.wordpress.org/Editing_wp-config.php
 *
 * @package WordPress
 */

// ** MySQL settings - You can get this info from your web host ** //
/** The name of the database for WordPress */
define('DB_NAME', 'mijondsa_db');

/** MySQL database username */
define('DB_USER', 'mijondsa_loco');

/** MySQL database password */
define('DB_PASSWORD', 'mijondsaghadb123');

/** MySQL hostname */
define('DB_HOST', 'localhost');

/** Database Charset to use in creating database tables. */
define('DB_CHARSET', 'utf8mb4');

/** The Database Collate type. Don't change this if in doubt. */
define('DB_COLLATE', '');

/**#@+
 * Authentication Unique Keys and Salts.
 *
 * Change these to different unique phrases!
 * You can generate these using the {@link https://api.wordpress.org/secret-key/1.1/salt/ WordPress.org secret-key service}
 * You can change these at any point in time to invalidate all existing cookies. This will force all users to have to log in again.
 *
 * @since 2.6.0
 */
define('AUTH_KEY',         'wEV3X|MJTyhoGHf#t1~Y$^y~*C/B?RGD)Lw5+`?MJ~YV=aBq|84tN(aQ{M]3X^xr');
define('SECURE_AUTH_KEY',  'ca4#L5:o-#Bzc_x@dgoeCkf>i{@BYKaBc._EX&B_;><|?b>7c|iJIx**[emZk#$$');
define('LOGGED_IN_KEY',    'v34-)%,K~.Wq)Skz1Uz1Ad=G([{_ZD 5+UCtRS}^!%C[?qKY{p0iKB}?%d}M&MmY');
define('NONCE_KEY',        '8:oP*93tvl!2Q8(] PzHmhP=/uHOe^.{ja|`91G2/_MpZzc(S!-S}][l7u%[{/7G');
define('AUTH_SALT',        '1D]80$2C0z$2#[K42R~RO/6B0jtr)Pn(1R)Z-4cE~@b}[=+DDe-U3TUK{dJxzZjb');
define('SECURE_AUTH_SALT', 'E}qYcnj$y41G)2nnz0lx||k^&#y#~dC}dN6/z>6N(3vy/lp za DhMlf(zBOHKvQ');
define('LOGGED_IN_SALT',   'JPFl#!jw]l>wb8: y0f7bj4}<ANJKuulSmBuL8!ogrylltK[2G;$igR)0KvJzms5');
define('NONCE_SALT',       ',7%,EboV1qK*%-h{UhF/o>>Sz3^WcU$ !u6S{[:_Vtrv<cJMGC]R8[@]=I)>Q7(l');

/**#@-*/

/**
 * WordPress Database Table prefix.
 *
 * You can have multiple installations in one database if you give each
 * a unique prefix. Only numbers, letters, and underscores please!
 */
$table_prefix  = 'wp_';

/**
 * For developers: WordPress debugging mode.
 *
 * Change this to true to enable the display of notices during development.
 * It is strongly recommended that plugin and theme developers use WP_DEBUG
 * in their development environments.
 *
 * For information on other constants that can be used for debugging,
 * visit the Codex.
 *
 * @link https://codex.wordpress.org/Debugging_in_WordPress
 */
define('WP_DEBUG', false);

/* That's all, stop editing! Happy blogging. */

/** Absolute path to the WordPress directory. */
if ( !defined('ABSPATH') )
	define('ABSPATH', dirname(__FILE__) . '/');

/** Sets up WordPress vars and included files. */
require_once(ABSPATH . 'wp-settings.php');
